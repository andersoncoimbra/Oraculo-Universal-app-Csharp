<Page
    x:Class="Oraculo.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Oraculo"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="Assets/bgrid.png"/>
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*" />

        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Width="auto" Header="Seu Nome" Margin="5,0,5,0" />
        <ComboBox x:Name="sig" ItemsSource="{Binding}" Grid.Row="1" Header="Signos" Margin="5,0,5,0">
            
        </ComboBox>
        <Button Grid.Row="2"  Content="Gerar Sorte" HorizontalAlignment="Center" Click="gera"/>
        <TextBlock x:Name="text" Grid.Row="3" FontSize="50" Text="Texto de mjiommimikmimik nhiuhiu Exemplo" VerticalAlignment="Center" HorizontalAlignment="Center" TextWrapping="Wrap"/>
        <Button Grid.Row="4"   Content="Sobre" VerticalAlignment="Bottom" HorizontalAlignment="Center" Width="300"/>
    </Grid>
</Page>